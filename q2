add code here
we will hopefully get it 100% fully functioning, you should be able to delete what i have written try it and let me know
#include <stdio.h>
#include <stdlib.h>

int main()
{

    int array2[4]={1,3,5,7};
    int loop2;
    int array[3]={11,8,4};
    int loop, smallest;

    smallest = array[0];

    for(loop  = 0; loop < 3; loop++){
        if(smallest > array[loop])
            smallest = array[loop];}
            printf(" Smallest value: %d ", smallest);
            for(loop2 = 0; loop2 < 4; loop2++){
                if(smallest == array2[loop2])

            if (smallest != array2[loop2]);
            smallest = array[loop];



            }

  return 0;
